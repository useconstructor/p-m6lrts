'use client'
import { FormEvent, useState } from 'react'; import { useRouter } from 'next/navigation'; import Link from 'next/link'; import { Building2 } from 'lucide-react'
export default function Login(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(false);const router=useRouter();async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError('');const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const d=await r.json();setLoading(false);if(!r.ok){setError(d.error);return}router.push('/admin')}return <main className="grid min-h-screen place-items-center bg-soft px-5"><div className="w-full max-w-md rounded-2xl border bg-white p-8 shadow-xl"><Link href="/" className="flex items-center gap-2 font-heading text-xl font-bold"><span className="grid size-8 place-items-center rounded-md bg-coral text-white"><Building2 className="size-4"/></span>Listwise</Link><h1 className="mt-10 font-heading text-3xl font-bold">Welcome back</h1><p className="mt-2 text-sm text-slate/60">Sign in to manage your property portfolio.</p><form onSubmit={submit} className="mt-8 space-y-5"><label className="block text-sm font-semibold">Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-2 w-full rounded-md border p-3 font-normal outline-none focus:border-coral"/></label><label className="block text-sm font-semibold">Password<input required minLength={8} type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-2 w-full rounded-md border p-3 font-normal outline-none focus:border-coral"/></label>{error&&<p className="rounded bg-red-50 p-3 text-sm text-red-700">{error}</p>}<button disabled={loading} className="button-primary w-full py-3">{loading?'Signing in…':'Sign in'}</button></form><Link href="/" className="mt-6 block text-center text-sm text-slate/60">Return to home</Link></div></main>}
